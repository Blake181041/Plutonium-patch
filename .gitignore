.freebuff/

# A Firebase service-account key must never be committed. Generate it locally,
# use it as an environment variable where it is needed, then delete the file.
*service-account*.json
*serviceAccount*.json
